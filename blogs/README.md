# Blog Content Folder

Add one Markdown file per blog post in this folder.

Required front matter fields:

- title
- date
- excerpt
- category
- readTime
- permalink

The build will generate each file into an HTML page at the permalink path.
