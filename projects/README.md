# Project Content Folder

Add one Markdown file per project write-up in this folder.

Suggested front matter fields:

- title
- date
- excerpt
- tech (array)
- permalink

The build will generate each file into an HTML page at the permalink path.
