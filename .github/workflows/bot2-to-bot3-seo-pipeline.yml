name: Bot 2 to Bot 3 SEO Pipeline

on:
  issue_comment:
    types: [created]

permissions:
  contents: read
  issues: write

jobs:
  bot3_from_bot2_comment:
    if: >-
      contains(github.event.issue.labels.*.name, 'write-blog') &&
      contains(github.event.comment.body, '<!-- bot-2-output -->') &&
      github.event.sender.login == 'github-actions[bot]'
    runs-on: ubuntu-latest

    steps:
      - name: Capture start time
        id: timing
        run: echo "start_epoch=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate Bot 3 output from Bot 2 marker
        run: |
          node scripts/bots/run-bot3-seo-refiner.js \
            --event "$GITHUB_EVENT_PATH" \
            --output "$RUNNER_TEMP/bot3-output.json" \
            --comment "$RUNNER_TEMP/bot3-comment.md"

      - name: Post Bot 3 comment
        if: success()
        uses: actions/github-script@v7
        env:
          START_EPOCH: ${{ steps.timing.outputs.start_epoch }}
        with:
          script: |
            const fs = require('fs');
            const runId = context.runId;
            const stage = 'bot3_from_bot2_comment';
            const startEpoch = Number(process.env.START_EPOCH || '0');
            const durationSec = startEpoch > 0 ? Math.max(0, Math.floor(Date.now() / 1000 - startEpoch)) : 0;
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${runId}`;

            let body = fs.readFileSync(`${process.env.RUNNER_TEMP}/bot3-comment.md`, 'utf8');
            const observability = [
              `- Stage: ${stage}`,
              `- Run ID: ${runId}`,
              `- Duration: ${durationSec}s`,
              `- Run URL: ${runUrl}`
            ].join('\n');

            body = body.replace(/(\*\*Bot 3 Result:[^\n]*\*\*)/, `$1\n\n${observability}`);

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });

      - name: Post workflow failure comment
        if: failure()
        uses: actions/github-script@v7
        env:
          START_EPOCH: ${{ steps.timing.outputs.start_epoch }}
        with:
          script: |
            const runId = context.runId;
            const stage = 'bot3_from_bot2_comment';
            const startEpoch = Number(process.env.START_EPOCH || '0');
            const durationSec = startEpoch > 0 ? Math.max(0, Math.floor(Date.now() / 1000 - startEpoch)) : 0;
            const runUrl = `${process.env.GITHUB_SERVER_URL}/${context.repo.owner}/${context.repo.repo}/actions/runs/${runId}`;

            const body = [
              '**Bot 3 Result: BLOCKED (workflow error)**',
              '',
              `- Stage: ${stage}`,
              `- Run ID: ${runId}`,
              `- Duration: ${durationSec}s`,
              `- Run URL: ${runUrl}`,
              '',
              '- Could not parse or validate the Bot 2 payload from this comment.',
              '- Ensure marker <!-- bot-2-output --> exists and contains valid JSON.',
              '- Accepted formats: raw JSON text, fenced JSON block, or plain fenced block.',
              '- Check workflow logs for exact error details.'
            ].join('\n');

            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body
            });
